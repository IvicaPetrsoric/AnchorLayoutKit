import XCTest
@testable import AnchorLayoutKit

final class AnchorLayoutKitTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(AnchorLayoutKit().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
